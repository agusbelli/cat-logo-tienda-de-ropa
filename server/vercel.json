{
  "version": 2,
  "builds": [
    {
      "src": "src/index.js",
      "use": "@vercel/node"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "build": {
    "commands": {
      "prisma:migrate": "npm run prisma:migrate",
      "prisma:generate": "npm run prisma:generate"
    }
  }
}
